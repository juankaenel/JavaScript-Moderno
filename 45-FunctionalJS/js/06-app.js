//Funciones puras -> retornan un dato pero no modifican los valores de la variable
const num1 = 20;

const duplicar = numero => numero *2; //no modifica el num1 sino que retorna un num nuevo
//toma un numero y retorna un numero, es decir retorna la cantidad de datos que vienen por parametro, si son dos retorna 2
//no modifican un valor original


const resultado = duplicar(resultado);
const resultado = duplicar(num1);