//Diferencia entre métodos y funciones

const numero1 = 20;
const numero2 = "20";

console.log(numero1.toString()); //Método
console.log(parseInt(numero2)); //Función  

//Prácticamente son equivalentes -> Las funciones se definen fuera de las clases, mientras que los Métodos se definen dentro y parte de las clases.