const prod = 'Pc Master Race';

console.log(prod.toLowerCase()); //pasame a minuscula todo el texto
console.log(prod.toUpperCase()); //pasame a mayúscula todo el texto

const precio1 = 3000;
console.log(precio1.toString()); //convierte a string