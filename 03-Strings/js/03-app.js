const producto = 'Monitor 32 pulgadas ';
const precio = '$30 pesos ARG'

console.log(producto.concat(precio));

console.log("el producto "+ producto + "tiene un precio de " + precio);

console.log(`El producto ${producto} tiene un precio de ${precio}`);